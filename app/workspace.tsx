'use client';
import { useEffect,useState,useMemo,useCallback } from 'react';
import { LayoutDashboard,Fingerprint,Telescope,Route,CalendarDays,Clapperboard,Users,Wallet,FolderOpen,BarChart3,ArrowUpRight,ArrowRight,LockKeyhole,Plus,Search,RefreshCw,Check,Link2,FileText,AlertCircle,ChevronRight,Save,Archive,ChevronLeft,List,Calendar,Target,ExternalLink,Rocket,Images,NotebookPen,ClipboardCheck } from 'lucide-react';
import { SidebarProvider,Sidebar,SidebarHeader,SidebarContent,SidebarFooter,SidebarMenu,SidebarMenuItem,SidebarMenuButton,SidebarGroup,SidebarGroupLabel,SidebarGroupContent,SidebarInset,SidebarTrigger,useSidebar } from '@/components/ui/sidebar';
import { Sheet,SheetContent,SheetHeader,SheetTitle,SheetDescription } from '@/components/ui/sheet';
import { Tabs,TabsList,TabsTrigger,TabsContent } from '@/components/ui/tabs';
import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';
import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from '@/components/ui/table';
import { AlertDialog,AlertDialogContent,AlertDialogHeader,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel,AlertDialogAction } from '@/components/ui/alert-dialog';
import { Toaster } from '@/components/ui/sonner';
import { toast } from 'sonner';
import { seed,statuses,currency,Entry } from './data';
import { areas,areaFor,attentionKind,attentionRecords,completeStatuses,enrichRecord } from './workflow';
const navGroups=[
 {label:'THE WORKSPACE',items:[['Overview',LayoutDashboard]]},
 {label:'BRAND',items:[['Brand & Business',Fingerprint],['Research',Telescope],['Strategy',Route]]},
 {label:'PLAN',items:[['Launch',Rocket],['Content Calendar',CalendarDays]]},
 {label:'CREATE',items:[['Production',Clapperboard],['Assets',Images],['Team & Vendors',Users]]},
 {label:'MANAGE',items:[['Scope & Budget',Wallet],['Decisions',ClipboardCheck],['Files',FolderOpen],['Reporting',BarChart3]]},
 {label:'INTERNAL',items:[['Private Planning',NotebookPen]]}
] as const;
const nav=navGroups.flatMap(g=>[...g.items]);
const aliases:Record<string,string>={'Brand & business':'Brand & Business','Strategy & launch':'Strategy','Content calendar':'Content Calendar','Team & vendors':'Team & Vendors','Scope & budgets':'Scope & Budget','Files & decisions':'Files'};
const destination=(hash:string)=>aliases[hash]||hash;
const descriptions:Record<string,string>={'Overview':'Retail ambition. Bespoke expertise. One connected plan.','Brand & Business':'The business, the people and the voice behind TWH.','Research':'Evidence first. A clear point of view follows.','Strategy':'What we choose to do, and why it matters.','Launch':'Turn the strategy into a coordinated route to market.','Content Calendar':'What we are publishing, when and why.','Production':'Plan the shoot. Account for every asset.','Assets':'Created, approved and delivered work, connected to its brief.','Team & Vendors':'The right people, with clear responsibilities.','Scope & Budget':'Know what is included, what is additional and what it costs.','Decisions':'Choices, approvals and the reasoning we need to remember.','Files':'The working documents and references behind the account.','Reporting':'Turn measured results into the next creative decision.','Private Planning':'A personal space for thinking before it becomes a workspace decision.'};
const phaseLabels=['Foundation','Awareness','Amplification','Conversion','Community','Launch'];
function Badge({value}:{value:unknown}){const s=String(value||'Not set');const tone=/Approved|Done|Published|confirmed|Reference/.test(s)?'teal':/review|Trial/.test(s)?'pink':/Awaiting|needed|clarification|Unassigned|Optional/.test(s)?'amber':'';return <span className={'pill '+tone}>{s}</span>}
function Choice({value,onChange,options,label}:{value:string;onChange:(v:string)=>void;options:string[];label:string}){return <Select value={value||'__empty'} onValueChange={v=>onChange(v==='__empty'?'':v)}><SelectTrigger aria-label={label}><SelectValue/></SelectTrigger><SelectContent><SelectItem value="__empty">Not set</SelectItem>{options.map(v=><SelectItem value={v} key={v}>{v}</SelectItem>)}</SelectContent></Select>}
function DateLabel({value}:{value:unknown}){if(!value)return <span className="muted">Unscheduled</span>;return <>{new Date(String(value)+'T12:00:00').toLocaleDateString('en-IN',{day:'numeric',month:'short',year:'numeric'})}</>}
function NavLinks({active,go}:{active:string;go:(n:string)=>void}){const {setOpenMobile}=useSidebar();return <>{navGroups.map(group=><SidebarGroup key={group.label}><SidebarGroupLabel>{group.label}</SidebarGroupLabel><SidebarGroupContent><SidebarMenu>{group.items.map(([n,Icon])=><SidebarMenuItem key={n}><SidebarMenuButton isActive={active===n} onClick={()=>{go(n);setOpenMobile(false)}}><Icon/><span>{n}</span></SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu></SidebarGroupContent></SidebarGroup>)}</>}
const fieldsets:Record<string,[string,string,string?][]>={
brand:[['lane','Business line','lane'],['notes','Positioning / working brief','textarea'],['source','Source / decision basis'],['url','Supporting document','url']],
research:[['url','Official brand / source link','url'],['offering','Offering','textarea'],['overview','Overview','textarea'],['tone','Tonality','textarea'],['pillars','Content pillars','textarea'],['ads','Observed ad strategy','textarea'],['works','What works','textarea'],['gaps','What does not work / opportunity','textarea'],['notes','Evidence, dates & implications for TWH','textarea']],
strategy:[['topic','Strategy topic','topic'],['phase','Growth phase','phase'],['date','Target date','date'],['owner','Accountable owner'],['notes','Strategic direction','textarea'],['gate','Readiness conditions','textarea'],['kpi','Success measures','textarea'],['service','Related service','relation:service'],['url','Strategy deck','url']],
task:[['area','Workstream','area'],['kind','Item type','kind'],['owner','Accountable owner'],['date','Due date','date'],['priority','Priority','priority'],['notes','Requirement / next action','textarea'],['url','Supporting link','url']],
content:[['track','Scope','track'],['cycle','Planning cycle'],['lane','Business line','lane'],['phase','Funnel / growth phase','phase'],['format','Format'],['channel','Publishing channels'],['date','Publishing date','date'],['deadline','Internal due date','date'],['owner','Assigned owner'],['approver','Client approver / approval note'],['hook','Opening hook','textarea'],['notes','Concept & creative direction','textarea'],['script','Script / talking-point skeleton','textarea'],['caption','Caption & copy','textarea'],['cta','Call to action'],['hypothesis','Trial hypothesis / learning objective','textarea'],['referenceUrl','Creative reference','url'],['assetUrl','Working asset / Drive folder','url'],['liveUrl','Live post URL','url'],['parent','Source content','relation:content'],['production','Production plan','relation:production'],['vendor','Vendor','relation:vendor'],['service','Cost allocated to','relation:service'],['budget','Planned asset cost (₹)','number'],['actual','Actual asset cost (₹)','number'],['results','Results & learning','textarea']],
production:[['owner','Production lead'],['date','Shoot / handover date','date'],['lane','Business line','lane'],['notes','Creative direction & production brief','textarea'],['shotlist','Shot list & run of show','textarea'],['assets','Equipment, props & asset allocation','textarea'],['deliverables','Handover deliverables','textarea'],['vendor','Assigned vendor','relation:vendor'],['service','Related service','relation:service'],['budget','Planned budget (₹)','number'],['actual','Actual cost (₹)','number'],['url','Master deck / Drive link','url']],
team:[['role','Role'],['email','Email','email'],['phone','Phone'],['capacity','Availability / capacity'],['notes','Responsibility & allocation','textarea']],
vendor:[['role','Specialism'],['contact','Contact person'],['email','Email','email'],['phone','Phone'],['location','Location'],['fee','Quoted fee (₹)','number'],['basis','Fee basis / inclusions'],['date','Availability date','date'],['url','Portfolio / quotation','url'],['notes','Capabilities, exclusions & payment notes','textarea']],
service:[['fee','Client fee / starting fee (₹)','number'],['basis','Commercial basis'],['owner','Service owner'],['notes','Included deliverables','textarea'],['excludes','Exclusions','textarea'],['reference','Rate-card reference / comparison','textarea'],['url','Approved scope / estimate','url']],
budget:[['fee','Monthly client fee (₹)','number'],['design','Design & editing allowance (₹)','number'],['subscriptions','Subscription allocation (₹)','number'],['people','Strategy, copy & account team cost (₹)','number'],['overhead','Overhead allocation (₹)','number'],['contingency','Contingency (₹)','number'],['notes','Assumptions','textarea']],
file:[['category','Document category'],['url','Canva / Drive / resource link','url'],['owner','Document owner'],['notes','Context & version notes','textarea']],
decision:[['area','Workstream','area'],['relatedTask','Related action','relation:task'],['outcome','Outcome / choice made','textarea'],['date','Decision / follow-up date','date'],['owner','Decision owner'],['source','Source / meeting'],['notes','Decision, rationale & impact','textarea'],['url','Supporting reference','url']],
launch:[['owner','Launch lead'],['date','Target milestone date','date'],['stage','Roadmap stage','stage'],['notes','Launch plan','textarea'],['gate','Readiness / go-live conditions','textarea'],['strategy','Related strategy','relation:strategy'],['production','Production plan','relation:production'],['url','Launch plan / working deck','url']],
asset:[['format','Asset type / format'],['lane','Business line','lane'],['owner','Asset owner'],['date','Delivery / review date','date'],['content','Source content brief','relation:content'],['production','Production plan','relation:production'],['assetUrl','Working file / final export','url'],['referenceUrl','Approval record','url'],['notes','Version, usage rights & handover notes','textarea']],
private:[['area','Related workstream','area'],['date','Follow-up date','date'],['notes','Private thinking / notes','textarea'],['url','Supporting link','url']],
report:[['cycle','Reporting period'],['date','Review date','date'],['owner','Report owner'],['reach','Reach','number'],['engagements','Engagements','number'],['enquiries','Qualified enquiries','number'],['sales','Attributable revenue (₹)','number'],['spend','Media spend (₹)','number'],['notes','Results, source & attribution limits','textarea'],['wins','What worked','textarea'],['next','What changes next month','textarea'],['url','Source report / analytics','url']]
};
export default function Dashboard(){
const [initialDraft,setInitialDraft]=useState(''),[closeRequest,setCloseRequest]=useState<{next:Entry|null}|null>(null);
const [active,setActive]=useState('Overview'),[records,setRecords]=useState<Entry[]>(seed.map(enrichRecord)),[ready,setReady]=useState(false),[loadError,setLoadError]=useState(''),[loading,setLoading]=useState(true),[draft,setDraft]=useState<Entry|null>(null),[saving,setSaving]=useState(false),[saveError,setSaveError]=useState(''),[query,setQuery]=useState(''),[filter,setFilter]=useState('All statuses'),[track,setTrack]=useState('Core'),[peopleTab,setPeopleTab]=useState('team'),[coordinationView,setCoordinationView]=useState('Open'),[strategyTab,setStrategyTab]=useState('Overview'),[attentionTab,setAttentionTab]=useState('All'),[showAllAttention,setShowAllAttention]=useState(false),[contentView,setContentView]=useState('list'),[month,setMonth]=useState(()=>new Date().toISOString().slice(0,7));
const load=useCallback(async()=>{setLoading(true);try{const r=await fetch('/api/records',{cache:'no-store'});const d=await r.json() as {error?:string;records:Entry[]};if(!r.ok)throw new Error(d.error);setRecords(d.records.map(enrichRecord));setReady(true);setLoadError('')}catch(e){setLoadError(e instanceof Error?e.message:'Could not load records');setReady(false)}finally{setLoading(false)}},[]);
useEffect(()=>{load();const hash=destination(decodeURIComponent(location.hash.slice(1)));if(nav.some(([n])=>n===hash))setActive(hash);const onHash=()=>{const n=destination(decodeURIComponent(location.hash.slice(1)));if(nav.some(([s])=>s===n)){setActive(n);setQuery('');setFilter('All statuses')}};window.addEventListener('hashchange',onHash);return()=>window.removeEventListener('hashchange',onHash)},[load]);
const go=useCallback((n:string)=>{setActive(n);setQuery('');setFilter('All statuses');setCoordinationView('Open');history.replaceState(null,'','#'+encodeURIComponent(n));window.scrollTo({top:0,behavior:'smooth'})},[]);
const list=(section:string)=>records.filter(r=>r.section===section&&r.status!=='Archived');
const filtered=(items:Entry[])=>items.filter(r=>(filter==='All statuses'?r.status!=='Archived':r.status===filter)&&JSON.stringify(r).toLowerCase().includes(query.toLowerCase()));
const open=useCallback((r:Entry)=>{setDraft({...r});setInitialDraft(JSON.stringify(r));setSaveError('')},[]);
function closeEditor(next:Entry|null=null){if(saving)return;if(draft&&JSON.stringify(draft)!==initialDraft){setCloseRequest({next});return}if(next)open(next);else setDraft(null)}
function add(section:string){open({id:crypto.randomUUID(),section,title:'',status:section==='content'?'Idea':'Proposed',version:0,...(section==='task'?{area:active==='Overview'?'Launch':active,kind:'Input',status:'Awaiting input'}:{}),...(section==='decision'?{area:active==='Overview'?'Strategy':active,status:'Awaiting approval'}:{}),...(section==='content'?{track,cycle:'Launch planning',service:track==='Trial'?'service-trial':'service-core'}:{})})}
async function save(){if(!draft||!ready)return;setSaveError('');if(!draft.title.trim()){setSaveError('Add a title before saving.');return}setSaving(true);try{const res=await fetch('/api/records',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(draft)});const d=await res.json() as {error?:string;record:Entry};if(!res.ok)throw new Error(d.error);setRecords(prev=>prev.some(r=>r.id===d.record.id)?prev.map(r=>r.id===d.record.id?d.record:r):[...prev,d.record]);setDraft(null);toast.success('Saved to the workspace')}catch(e){setSaveError(e instanceof Error?e.message:'Could not save. Your edits are still here.')}finally{setSaving(false)}}
useEffect(()=>{const context=(document as unknown as {modelContext?:{registerTool:(t:unknown,o:unknown)=>void}}).modelContext;if(!context)return;const controller=new AbortController();try{context.registerTool({name:'read_twh_records',title:'Read TWH workspace records',description:'Read the current saved TWH records, optionally filtered by section. Does not change data.',inputSchema:{type:'object',properties:{section:{type:'string'}},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},execute:(input:unknown)=>{const x=input as {section?:string};if(x.section&&!Object.keys(fieldsets).includes(x.section))throw new Error('Unknown section');return {ready,records:records.filter(r=>!x.section||r.section===x.section)}}},{signal:controller.signal});context.registerTool({name:'open_twh_record',title:'Open a TWH record for editing',description:'Open an existing record in the visible editor. Does not save changes or approve work.',inputSchema:{type:'object',properties:{id:{type:'string'}},required:['id'],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:true},execute:(input:unknown)=>{if(draft&&JSON.stringify(draft)!==initialDraft)throw new Error('Save or discard the current draft before opening another record.');const id=(input as {id:string}).id;const r=records.find(v=>v.id===id);if(!r)throw new Error('Record not found');open(r);return {opened:id,title:r.title}}},{signal:controller.signal})}catch(e){console.warn('Workspace tools unavailable',e)}return()=>controller.abort()},[records,ready,open,draft,initialDraft]);
const budget=list('budget')[0],core=list('content').filter(r=>r.track==='Core'),trial=list('content').filter(r=>r.track==='Trial');
const totalCost=['design','subscriptions','people','overhead','contingency'].reduce((sum,k)=>sum+Number(budget?.[k]||0),0);
const outstanding=attentionRecords(records);
const currentDay=new Date().toLocaleDateString('en-CA');
const dated=records.filter(r=>r.section!=='private'&&!completeStatuses.includes(r.status)&&(r.deadline||r.date)).sort((a,b)=>String(a.deadline||a.date).localeCompare(String(b.deadline||b.date)));
const recentDecisions=list('decision').filter(r=>['Approved','Done','Brief confirmed'].includes(r.status)).sort((a,b)=>String(b.date||b.updatedAt||'').localeCompare(String(a.date||a.updatedAt||'')));
const due=(r:Entry)=>String(r.deadline||r.date||'');

function Heading({title,sub,action,actionLabel}:{title:string;sub?:string;action?:()=>void;actionLabel?:string}){return <div className="page-heading"><div><p className="eyebrow">TWH / {active.toUpperCase()}</p><h1>{title}</h1><p className="muted">{sub||descriptions[active]}</p></div>{action&&<button className="button primary" onClick={action}><Plus size={16}/>{actionLabel||'Add record'}</button>}</div>}
function Note({children}:{children:React.ReactNode}){return <div className="info-note"><AlertCircle size={17}/><p>{children}</p></div>}
function Cards({items}:{items:Entry[]}){return <div className="card-grid">{items.map(r=><button key={r.id} className="record-card" onClick={()=>open(r)}><div className="card-top"><Badge value={r.status}/><ArrowUpRight size={17}/></div><h3>{r.title}</h3>{r.role&&<p className="teal-text">{r.role}</p>}{r.fee&&<strong className="price">{currency(r.fee)}<small>{r.basis||'Quote to confirm'}</small></strong>}<p className="clamp">{r.notes}</p><div className="card-foot"><span>{r.owner||r.lane||r.category||r.phase||'Open details'}</span>{r.date?<DateLabel value={r.date}/>:<span>View brief <ChevronRight size={13}/></span>}</div></button>)}</div>}
function renderToolbar({placeholder='Search this section…'}:{placeholder?:string}){return <div className="toolbar"><div className="search"><Search size={17}/><input aria-label="Search records" placeholder={placeholder} value={query} onChange={e=>setQuery(e.target.value)}/></div><Choice label="Filter by status" value={filter} onChange={setFilter} options={['All statuses',...statuses]}/></div>}
function Empty({text,section,detail}:{text:string;section?:string;detail?:string}){return <div className="empty-state"><FolderOpen size={26}/><h3>{text}</h3><p>{detail||'Add the confirmed information when it is available.'}</p>{section&&<button className="button" onClick={()=>add(section)}><Plus size={16}/>Add {section==='vendor'?'vendor':section==='report'?'report':'record'}</button>}</div>}
function Rows({items,content=false}:{items:Entry[];content?:boolean}){return items.length?<div className="table-wrap"><Table><TableHeader><TableRow><TableHead>{content?'Content / concept':'Requirement / record'}</TableHead><TableHead>Status</TableHead><TableHead>{content?'Business line':'Owner'}</TableHead><TableHead>{content?'Format':'Priority'}</TableHead><TableHead>{content?'Publish date':'Due date'}</TableHead><TableHead><span className="sr-only">Open</span></TableHead></TableRow></TableHeader><TableBody>{items.map(r=><TableRow key={r.id}><TableCell><button className="table-title" onClick={()=>open(r)}>{r.title}</button><small>{content?(r.owner||'Owner to assign'):(r.notes||'').toString().slice(0,85)}</small></TableCell><TableCell><Badge value={r.status}/></TableCell><TableCell>{content?r.lane:r.owner||'Unassigned'}</TableCell><TableCell>{content?r.format:r.priority||'—'}</TableCell><TableCell><DateLabel value={r.date}/></TableCell><TableCell><button className="icon-button" aria-label={'Open '+r.title} onClick={()=>open(r)}><ArrowUpRight size={17}/></button></TableCell></TableRow>)}</TableBody></Table></div>:<Empty text="No matching records"/>}
function renderWorkItems({area}:{area:string}){const items=coordinationView==='Open'?outstanding.filter(r=>areaFor(r)===area):records.filter(r=>['task','decision'].includes(r.section)&&areaFor(r)===area&&completeStatuses.includes(r.status));return <section className="panel section-gap"><div className="section-head"><div><p className="eyebrow">COORDINATION</p><h2>Inputs & decisions</h2></div><button className="text-button" onClick={()=>add('task')}>Add input / action <Plus size={15}/></button></div><Tabs value={coordinationView} onValueChange={setCoordinationView}><TabsList><TabsTrigger value="Open">Open</TabsTrigger><TabsTrigger value="History">History</TabsTrigger></TabsList></Tabs>{items.length?<AttentionRows items={items}/>:<p className="quiet-line">{coordinationView==='Open'?'No open inputs, decisions or blockers in this workstream.':'No completed or archived items in this workstream yet.'}</p>}</section>}
function AttentionRows({items}:{items:Entry[]}){return <div className="attention-list">{items.map(r=><div className="attention-row" key={r.id}><button className="attention-main" onClick={()=>open(r)}><span className={'attention-dot '+(r.status==='Blocked'?'blocked':'')}/><div><strong>{r.title}</strong><small>{r.owner||'Owner to assign'}{due(r)&&<> · <span className={due(r)<currentDay?'overdue':''}>{due(r)<currentDay?'Overdue · ':''}<DateLabel value={due(r)}/></span></>}</small></div></button><button className="area-link" onClick={()=>go(areaFor(r))}>{areaFor(r)} <ArrowUpRight size={12}/></button><Badge value={r.status}/><button className="icon-button" aria-label={'Review '+r.title} onClick={()=>open(r)}><ArrowUpRight size={16}/></button></div>)}</div>}
function Roadmap(){const groups=[{label:'NOW',title:'Build the foundation',note:'Positioning, product clarity and the launch direction.',ids:['strategy-foundation']},{label:'NEXT',title:'Bring the launch to life',note:'Launch planning → production → awareness and trial learning.',ids:['launch-plan','launch-production','strategy-awareness']},{label:'LATER',title:'Grow what works',note:'Boosting → website-ready performance → trade community.',ids:['strategy-boost','strategy-performance','strategy-community']}];return <section className="panel section-gap"><div className="section-head"><div><p className="eyebrow">DIRECTION INTO DELIVERY</p><h2>Current roadmap</h2></div><button className="text-button" onClick={()=>go('Strategy')}>View strategy <ArrowRight size={16}/></button></div><div className="journey">{groups.map(g=><article key={g.label}><span className="eyebrow">{g.label}</span><h3>{g.title}</h3><p>{g.note}</p>{g.ids.map(id=>{const r=records.find(x=>x.id===id&&x.status!=='Archived');return r?<button key={id} onClick={()=>open(r)}><span>{r.title.replace(/^\d+ · /,'')}</span><Badge value={r.status}/></button>:null})}</article>)}</div></section>}
function CalendarGrid({items}:{items:Entry[]}){const [y,m]=month.split('-').map(Number);const count=new Date(y,m,0).getDate();const offset=(new Date(y,m-1,1).getDay()+6)%7;return <><div className="calendar-controls"><input aria-label="Calendar month" type="month" value={month} onChange={e=>e.target.value&&setMonth(e.target.value)}/><span className="muted">{items.filter(r=>!r.date).length} concepts are unscheduled. Open a concept to set its date.</span></div><div className="calendar-grid">{['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map(d=><strong className="weekday" key={d}>{d}</strong>)}{Array.from({length:offset},(_,i)=><div className="calendar-day blank" key={'b'+i}/>)}{Array.from({length:count},(_,i)=>{const date=month+'-'+String(i+1).padStart(2,'0');return <div className="calendar-day" key={date}><span>{i+1}</span>{items.filter(r=>r.date===date).map(r=><button onClick={()=>open(r)} key={r.id}>{r.title}<small>{r.status}</small></button>)}</div>})}</div>{items.every(r=>!r.date)&&<Note>No publishing dates have been supplied. The proposed concepts remain in the list view until scheduled.</Note>}</>}
let content:React.ReactNode;
switch(active){
case 'Overview':{const attention=outstanding.filter(r=>attentionTab==='All'||attentionKind(r)===attentionTab);content=<>
<Heading title="A new chapter. In motion." action={()=>go('Content Calendar')} actionLabel="Open calendar"/>
<div className="metrics"><article><p>Core pieces</p><strong>{core.length} <small>pieces</small></strong><span>{core.filter(r=>r.status==='Published').length} published · monthly scope</span></article><article><p>Trial pieces</p><strong>{trial.length} <small>extra</small></strong><span>Separate scope & fee</span></article><article><p>Proposed monthly retainer</p><strong>{new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',notation:'compact',maximumFractionDigits:1}).format(Number(records.find(r=>r.id==='service-core')?.fee)||0)}</strong><span>Excluding GST · approval pending</span></article><article className="action-metric"><p>Open actions</p><strong>{outstanding.length.toString().padStart(2,'0')}</strong><span>{outstanding.filter(r=>r.status==='Blocked').length} blockers · across all workstreams</span></article></div>
<section className="panel attention-panel"><div className="section-head"><div><h2>Needs your attention</h2><p className="section-sub">{outstanding.length} open items · {outstanding.filter(r=>/TWH|Tanya/.test(String(r.owner||''))&&r.status==='Awaiting input').length} waiting on TWH</p></div><button className="text-button" onClick={()=>add('task')}>Add input / action <Plus size={15}/></button></div>
<Tabs value={attentionTab} onValueChange={v=>{setAttentionTab(v);setShowAllAttention(false)}}><TabsList className="attention-tabs">{['All','Inputs','Decisions','Blockers','Actions'].map(t=><TabsTrigger value={t} key={t}>{t} <span>{outstanding.filter(r=>t==='All'||attentionKind(r)===t).length}</span></TabsTrigger>)}</TabsList></Tabs>
{attention.length?<AttentionRows items={showAllAttention?attention:attention.slice(0,3)}/>:<p className="quiet-line">No open {attentionTab==='All'?'items':attentionTab.toLowerCase()}.</p>}
{attention.length>3&&<button className="text-button attention-more" onClick={()=>setShowAllAttention(!showAllAttention)}>{showAllAttention?'Show fewer':`View all ${attention.length}`} <ArrowRight size={15}/></button>}
</section>
<div className="overview-grid section-gap"><section className="panel"><div className="section-head"><h2>Where we’re going</h2><span className="eyebrow">STRATEGIC EMPHASIS</span></div><div className="balance"><div><span className="teal-text">RETAIL</span><strong>65<span>%</span></strong><p>Build desire. Make buying clear.</p></div><div><span className="pink-text">BESPOKE / EXPERIENCE</span><strong>35<span>%</span></strong><p>Show expertise. Drive consultations.</p></div></div><div className="split-bar"><span/><span/></div><p className="footnote">Strategic priority, not a fixed post or spend split. Trade connects both.</p></section>
<section className="panel"><div className="section-head"><h2>Coming up</h2><CalendarDays size={19}/></div>{dated.length?dated.slice(0,4).map(r=><button className="todo" key={r.id} onClick={()=>open(r)}><div><strong>{r.title}</strong><small>{areaFor(r)} · <span className={due(r)<currentDay?'overdue':''}>{due(r)<currentDay?'Overdue · ':''}<DateLabel value={due(r)}/></span></small></div><ArrowUpRight size={16}/></button>):<div className="quiet-state"><CalendarDays size={21}/><p>Dates are still to be confirmed.<small>Due dates and publishing dates appear here as soon as they are assigned.</small></p></div>}</section></div>
<Roadmap/><div className="overview-grid lower-grid"><section className="panel"><div className="section-head"><h2>Recent decisions</h2><button className="text-button" onClick={()=>go('Decisions')}>Decision log <ArrowRight size={15}/></button></div>{recentDecisions.length?recentDecisions.slice(0,3).map(r=><button key={r.id} className="todo" onClick={()=>open(r)}><div><strong>{r.title}</strong><small>{areaFor(r)} · {r.status}{r.date&&<> · <DateLabel value={r.date}/></>}</small></div><ArrowUpRight size={16}/></button>):<p className="muted">No confirmed decisions recorded yet.</p>}</section><section className="panel"><div className="section-head"><h2>Working references</h2><button className="text-button" onClick={()=>go('Files')}>All files <ArrowRight size={16}/></button></div>{list('file').slice(0,3).map(r=><a key={r.id} className="file-row" href={String(r.url)} target="_blank" rel="noreferrer"><FileText size={17}/><span>{r.title}</span><ExternalLink size={14}/></a>)}</section></div></>;break;}
case 'Brand & Business':content=<><Heading title="The world of TWH." action={()=>add('brand')} actionLabel="Add brand note"/><Note>The business brief is confirmed by your instructions. Personality and language are working directions for validation with Tanya.</Note><Cards items={list('brand')}/><section className="panel section-gap"><div className="section-head"><h2>What every piece of communication must preserve</h2><Fingerprint size={20}/></div><div className="principles"><div><b>01</b><h3>One coherent identity</h3><p>Retail and bespoke should feel related while their specifications and level of customisation remain clear.</p></div><div><b>02</b><h3>Expertise with personality</h3><p>Tanya’s real beliefs and decisions anchor the founder IP. Hooks support her voice; they do not replace it.</p></div><div><b>03</b><h3>Proof over invention</h3><p>Use verified product details, approved archives and actual testimonials. Conceptual AI scenes are distinct from delivered projects.</p></div></div></section></>;break;
case 'Research':{const rs=filtered(records.filter(r=>r.section==='research'));content=<><Heading title="Find the meaningful difference." action={()=>add('research')} actionLabel="Add reference"/><Note>The eight competitors are from TWH’s brief. Analysis is pending; no ad results or positioning claims have been invented. Confirm IDES / IDUS first.</Note>{renderToolbar({})}<Cards items={rs}/>{!rs.length&&<Empty text="No matching references"/>}</>;break;}
case 'Strategy':{const strategyItems=list('strategy').filter(r=>r.phase!=='Launch');const selected=strategyTab==='Overview'?strategyItems:strategyItems.filter(r=>r.topic===strategyTab);const contextItems=strategyTab==='Positioning'?list('brand').filter(r=>r.id==='brand-model'):strategyTab==='Audience'?list('brand').filter(r=>['brand-retail','brand-bespoke','brand-trade'].includes(r.id)):strategyTab==='Messaging'?list('brand').filter(r=>r.id==='brand-personality'):strategyTab==='Opportunities'?strategyItems.filter(r=>['Amplification','Conversion','Community'].includes(String(r.phase))):[];content=<><Heading title="A direction worth following." action={()=>add('strategy')} actionLabel="Add strategy"/><Tabs value={strategyTab} onValueChange={setStrategyTab}><TabsList className="section-tabs">{['Overview','Positioning','Audience','Messaging','Opportunities'].map(t=><TabsTrigger key={t} value={t}>{t}</TabsTrigger>)}</TabsList></Tabs><div className="section-gap"><Cards items={[...new Map([...contextItems,...selected].map(r=>[r.id,r])).values()]}/></div>{renderWorkItems({area:'Strategy'})}<section className="panel section-gap"><div className="section-head"><h2>Ready to bring the direction to market?</h2><button className="text-button" onClick={()=>go('Launch')}>Open launch plan <ArrowRight size={16}/></button></div><p className="muted">Launch owns milestones, readiness and coordination across content, production and offline touchpoints.</p></section></>;break;}
case 'Launch':content=<><Heading title="Bring the next chapter to market." action={()=>add('launch')} actionLabel="Add milestone"/><div className="process-strip">Direction <ChevronRight/> Launch planning <ChevronRight/> Content approval <ChevronRight/> Production <ChevronRight/> Go live <ChevronRight/> Learning</div><Note>Launch and website dates are unconfirmed. These are proposed milestones, with readiness conditions to complete before going live.</Note>{renderToolbar({})}<Cards items={filtered(records.filter(r=>r.section==='launch'||(r.section==='strategy'&&r.phase==='Launch')))}/>{renderWorkItems({area:'Launch'})}</>;break;
case 'Content Calendar':{const items=filtered(records.filter(r=>r.section==='content'&&r.track===track));content=<><Heading title="Ideas, made actionable." action={()=>add('content')} actionLabel="Add content"/><Note>30 suggested starting concepts: 15 core pieces + 15 separately scoped trial variants. Dates, final copy and client approvals are not supplied. Nothing is scheduled to social platforms from this workspace.</Note><div className="tabs-with-action"><Tabs value={track} onValueChange={v=>{setTrack(v);setQuery('')}}><TabsList><TabsTrigger value="Core">Core calendar <span>{core.length}</span></TabsTrigger><TabsTrigger value="Trial">Trial reels <span>{trial.length}</span></TabsTrigger></TabsList></Tabs><div className="view-switch"><button className={contentView==='list'?'selected':''} aria-label="List view" onClick={()=>setContentView('list')}><List size={17}/></button><button className={contentView==='calendar'?'selected':''} aria-label="Calendar view" onClick={()=>setContentView('calendar')}><Calendar size={17}/></button></div></div>{renderToolbar({placeholder:'Search concepts, hooks, owners…'})}{contentView==='list'?<Rows items={items} content/>:<CalendarGrid items={items}/>}</>;break;}
case 'Production':content=<><Heading title="Everything before the camera." action={()=>add('production')} actionLabel="Add production plan"/><div className="brief-strip"><Clapperboard size={22}/><p><strong>Founder shoot target: ₹8,000–₹10,000 / day.</strong> Gurgaon · mobile capture · vendor and inclusions to confirm.</p></div>{renderToolbar({})}<Cards items={filtered(records.filter(r=>r.section==='production'))}/><section className="panel section-gap"><h2>The handover standard</h2><p className="body-copy">Each plan connects the approved calendar to scripts, shots, equipment, owners, a vendor, budget and a working folder. Confirm footage rights, source files, audio quality, backups and delivery format before the shoot. Advanced motion, green-screen compositing and specialist AI work need explicit allocation.</p></section></>;break;
case 'Team & Vendors':{const items=filtered(records.filter(r=>r.section===peopleTab));content=<><Heading title="People behind the work." action={()=>add(peopleTab)} actionLabel={peopleTab==='team'?'Add team member':'Add vendor'}/><Tabs value={peopleTab} onValueChange={v=>{setPeopleTab(v);setQuery('');setFilter('All statuses')}}><TabsList><TabsTrigger value="team">Team allocation</TabsTrigger><TabsTrigger value="vendor">Vendor directory</TabsTrigger></TabsList></Tabs>{renderToolbar({placeholder:'Search names, roles, locations…'})}{peopleTab==='team'&&<Note>Simran and Sharon’s roles come from Plutonik’s credentials. TWH allocations are proposed; capacity and other team members still need confirmation.</Note>}{items.length?<Cards items={items}/>:<Empty text={peopleTab==='vendor'?'Your vendor directory starts here':'No matching team members'} section={peopleTab}/>}</>;break;}
case 'Scope & Budget':content=<><Heading title="Clear scope. Considered spend." action={()=>add('service')} actionLabel="Add service"/><div className="budget-layout"><section className="panel"><div className="section-head"><h2>Monthly retainer model</h2><button className="text-button" onClick={()=>open(budget)}>Edit assumptions <ArrowUpRight size={16}/></button></div><div className="budget-line"><span>Proposed client fee</span><strong>{currency(budget?.fee)}</strong></div>{[['design','Design & editing'],['subscriptions','Subscriptions'],['people','Strategy, copy & account team'],['overhead','Overhead'],['contingency','Contingency']].map(([key,label])=><div className="budget-line" key={key}><span>{label}</span><span>{budget?.[key]!==''&&budget?.[key]!==undefined?currency(budget[key]):'Not entered'}</span></div>)}<div className="budget-total"><span>Balance after entered costs</span><strong>{currency(Number(budget?.fee||0)-totalCost)}</strong></div><p className="footnote">Not profit. Unentered costs remain outstanding. GST and separately scoped work are excluded.</p></section><section className="panel commercial-note"><span className="eyebrow">RATE CARD → TWH</span><h2>A tailored 15-piece scope.</h2><p>The standard package has 12 outputs. TWH proposes 15 core pieces at ₹1.20L/month, with the 15 trial reels costed separately.</p><p>Lock the format mix, story volume, revisions, term, strategy cadence and pre-production boundary before approval.</p><a className="text-button" href="https://www.canva.com/design/DAHT15xjJiU" target="_blank" rel="noreferrer">Open Plutonik rate card <ExternalLink size={15}/></a></section></div><div className="section-head section-gap"><h2>Services for this account</h2><span className="footnote">Starting fees / proposals · GST additional</span></div>{renderToolbar({})}<Cards items={filtered(records.filter(r=>r.section==='service'))}/></>;break;
case 'Assets':{const items=filtered(records.filter(r=>r.section==='asset'||(r.section==='content'&&(r.assetUrl||r.liveUrl))));content=<><Heading title="The work, all in one place." action={()=>add('asset')} actionLabel="Add asset"/><div className="asset-summary">{['In progress','Approved','Delivered'].map(status=><div key={status}><strong>{items.filter(r=>status==='In progress'?!['Approved','Delivered','Published'].includes(r.status):status==='Delivered'?['Delivered','Published'].includes(r.status):r.status===status).length}</strong><span>{status==='In progress'?'Working assets':status}</span></div>)}</div>{renderToolbar({placeholder:'Search asset names, formats, owners…'})}{items.length?<div className="file-grid">{items.map(r=><article className="file-card" key={r.id}><Images size={24}/><Badge value={r.status}/><h3>{r.title}</h3><small>{r.format||'Asset'} · {r.owner||'Owner to assign'}</small><div><button className="text-button" onClick={()=>open(r)}>Details / edit</button>{(r.assetUrl||r.liveUrl)&&<a className="text-button" href={String(r.assetUrl||r.liveUrl)} target="_blank" rel="noreferrer">Open asset <ExternalLink size={15}/></a>}</div></article>)}</div>:<Empty text="The asset library is ready for the first handover" section="asset" detail="Add a working file or final export with its status and source brief. Content with a working asset or live link appears here automatically."/>}{renderWorkItems({area:'Assets'})}</>;break;}
case 'Decisions':{const items=filtered(records.filter(r=>r.section==='decision'));content=<><Heading title="Make the call. Keep the context." action={()=>add('decision')} actionLabel="Record decision"/><Note>Inputs ask for information. Decisions record a choice, who owns it and why. Pending decisions also appear on Overview.</Note>{renderToolbar({placeholder:'Search decisions, owners and rationale…'})}{items.length?<div className="table-wrap"><Table><TableHeader><TableRow><TableHead>Decision</TableHead><TableHead>Area</TableHead><TableHead>Owner</TableHead><TableHead>Status</TableHead><TableHead>Decision / follow-up date</TableHead></TableRow></TableHeader><TableBody>{items.map(r=><TableRow key={r.id}><TableCell><button className="table-title" onClick={()=>open(r)}>{r.title}</button><small>{r.outcome||r.source||'Rationale inside the record'}</small></TableCell><TableCell><button className="area-link" onClick={()=>go(areaFor(r))}>{areaFor(r)}</button></TableCell><TableCell>{r.owner||'Owner to assign'}</TableCell><TableCell><Badge value={r.status}/></TableCell><TableCell>{r.date?<DateLabel value={r.date}/>:<span className="muted">Not recorded</span>}</TableCell></TableRow>)}</TableBody></Table></div>:<Empty text="No matching decisions"/>}</>;break;}
case 'Files':{const items=filtered(records.filter(r=>r.section==='file'));content=<><Heading title="Keep the working material close." action={()=>add('file')} actionLabel="Add link"/>{renderToolbar({placeholder:'Search documents, decks and references…'})}<div className="file-grid">{items.map(r=><article className="file-card" key={r.id}><FileText size={24}/><small>{r.category||'Resource'}</small><h3>{r.title}</h3><div><button className="text-button" onClick={()=>open(r)}>Details / edit</button>{r.url&&<a className="text-button" href={String(r.url)} target="_blank" rel="noreferrer">Open <ExternalLink size={15}/></a>}</div></article>)}</div>{!items.length&&<Empty text="No matching files"/>}<Note>Files holds decks, documents and working folders. Finished creative belongs in Assets; approvals and choices belong in Decisions. Canva and Drive links open their source and do not automatically sync.</Note>{renderWorkItems({area:'Files'})}</>;break;}
case 'Private Planning':{const items=filtered(records.filter(r=>r.section==='private'));content=<><Heading title="Room to think ahead." action={()=>add('private')} actionLabel="Add private note"/><div className="brief-strip"><LockKeyhole size={20}/><p><strong>Notes here are visible only to your signed-in account.</strong> They stay out of Overview and the shared workstreams. Other sections follow the workspace’s access settings.</p></div>{renderToolbar({placeholder:'Search your private notes…'})}{items.length?<Cards items={items}/>:<Empty text="Your thinking space starts here" section="private" detail="Keep early ideas, negotiation notes and personal follow-ups here. Record a separate decision when the direction is ready for the workspace."/>}</>;break;}
case 'Reporting':content=<><Heading title="What the work is teaching us." action={()=>add('report')} actionLabel="Add monthly report"/><div className="report-intro"><div><Target size={25}/><h2>Measure the business journey.</h2><p>Awareness → consideration → qualified enquiries → consultations or retail sales. Record data sources and attribution limits.</p></div><div className="metric-definitions"><span>Retail <b>Product interest & purchases</b></span><span>Bespoke <b>Consultations & qualified projects</b></span><span>Trade <b>Relevant relationships & enquiries</b></span></div></div>{list('report').length?<Cards items={list('report')}/>:<Empty text="No performance data recorded yet" section="report"/>}<section className="panel section-gap"><h2>Close the loop</h2><p className="body-copy">Record trial-reel findings on each variant, identify which hooks and topics deserve further testing, and feed the monthly report into the next calendar. Boosting and performance spend should be assessed against their own objectives. Sales follow-up remains a named responsibility even when ORM is not included.</p></section></>;break;
default:content=null;
}
return <SidebarProvider><Sidebar><SidebarHeader><button className="wordmark" onClick={()=>go('Overview')} aria-label="Plutonik account overview">PLUTONIK<span>CREATIVE HOUSE</span></button><div className="account"><b>T</b><div><strong>TWH</strong><small>Client workspace</small></div><span className="account-orbit">◌</span></div></SidebarHeader><SidebarContent><NavLinks active={active} go={go}/></SidebarContent><SidebarFooter><div className="private"><LockKeyhole size={14}/> Private workspace</div><div className="identity"><b>P</b><div>Plutonik team<small>Strategy · Creation · Delivery</small></div></div></SidebarFooter></Sidebar><SidebarInset><header className="topbar"><div><SidebarTrigger/><span>Clients</span><span>/</span><strong>TWH</strong></div><div><span className="save-state">{loading?'Loading records…':ready?'Workspace saved':'Read-only brief'}</span><button className="icon-button" onClick={load} aria-label="Refresh workspace" disabled={loading}><RefreshCw size={16}/></button></div></header><div className="workspace">{loadError&&<div className="error-note" role="alert"><AlertCircle size={18}/><p>{loadError}</p><button onClick={load}>Retry</button></div>}{content}{['Brand & Business','Research','Content Calendar','Production','Team & Vendors','Scope & Budget','Reporting'].includes(active)&&renderWorkItems({area:active})}<div className="bottom-note"><span>Working brief · proposals and confirmed decisions are labelled separately.</span><span>PLUTONIK × TWH</span></div></div></SidebarInset><Sheet open={!!draft} onOpenChange={v=>{if(!v)closeEditor()}}><SheetContent className="editor" side="right"><SheetHeader><div className="eyebrow">TWH / {draft?.section}</div><SheetTitle>{draft?.title||(draft?'New '+draft.section:'Record details')}</SheetTitle><SheetDescription>Update the working record. Saving does not publish content or approve vendor spending.</SheetDescription></SheetHeader>{draft&&<form className="editor-form" onSubmit={e=>{e.preventDefault();save()}}><label className="field"><span>Title / name <b>*</b></span><input required maxLength={200} value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})}/></label><label className="field"><span>Status</span><Choice label="Record status" value={draft.status} options={statuses} onChange={status=>setDraft({...draft,status})}/></label>{(fieldsets[draft.section]||[]).map(([key,label,type])=>{const options=type==='area'?areas:type==='kind'?['Input','Action','Decision']:type==='topic'?['Positioning','Audience','Messaging','Opportunities']:type==='stage'?['Now','Next','Later']:type==='lane'?['Retail','Bespoke','Trade','Both']:type==='phase'?phaseLabels:type==='priority'?['High','Medium','Low']:type==='track'?['Core','Trial']:undefined;const rel=type?.startsWith('relation:')?type.split(':')[1]:'';return <label className={'field '+(type==='textarea'?'wide':'')} key={key}><span>{label}</span>{options?<Choice label={label} value={String(draft[key]||'')} options={options} onChange={v=>setDraft({...draft,[key]:v})}/>:rel?<Select value={String(draft[key]||'__none')} onValueChange={v=>setDraft({...draft,[key]:v==='__none'?'':v})}><SelectTrigger aria-label={label}><SelectValue/></SelectTrigger><SelectContent><SelectItem value="__none">Not linked</SelectItem>{records.filter(r=>r.section===rel&&r.id!==draft.id&&r.status!=='Archived').map(r=><SelectItem key={r.id} value={r.id}>{r.title}</SelectItem>)}</SelectContent></Select>:type==='textarea'?<textarea rows={key==='script'?6:4} value={String(draft[key]||'')} onChange={e=>setDraft({...draft,[key]:e.target.value})}/>:<input type={type||'text'} min={type==='number'?0:undefined} step={type==='number'?'any':undefined} value={String(draft[key]??'')} onChange={e=>setDraft({...draft,[key]:e.target.value})}/>}</label>})}{['content','asset','launch','decision'].includes(draft.section)&&<div className="linked-records wide">{['parent','production','vendor','service','content','strategy','relatedTask'].map(key=>{const target=records.find(r=>r.id===draft[key]);return target?<button type="button" key={key} className="text-button" onClick={()=>closeEditor(target)}><Link2 size={14}/>{target.title}<ArrowUpRight size={14}/></button>:null})}<small>Linked records keep the brief, decisions and delivery connected.</small></div>}{saveError&&<p className="error-note wide" role="alert">{saveError}</p>}<div className="editor-actions wide"><button type="button" className="button" onClick={()=>closeEditor()} disabled={saving}>Close</button><button className="button primary" type="submit" disabled={!ready||saving}><Save size={16}/>{saving?'Saving…':'Save changes'}</button></div>{!ready&&<p className="footnote wide">Saving will be available after the workspace connects. Use Refresh workspace to retry.</p>}</form>}</SheetContent></Sheet><AlertDialog open={!!closeRequest} onOpenChange={v=>{if(!v)setCloseRequest(null)}}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Keep your changes?</AlertDialogTitle><AlertDialogDescription>This record has unsaved edits. Keep editing to save them, or discard this draft.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Keep editing</AlertDialogCancel><AlertDialogAction onClick={()=>{const next=closeRequest?.next;setCloseRequest(null);if(next)open(next);else setDraft(null)}}>Discard draft</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog><Toaster position="bottom-right"/></SidebarProvider>;
}
